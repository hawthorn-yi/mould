# 错误经验

> 说明：后续执行任务时先看这里，避免重复犯错。新错误会自动补录。

## 2026-08-15

- GitHub `git push` 连接 `github.com` 被重置或超时，后续优先尝试 GitHub Contents API 上传文件。
- Supabase 的 `anon key` 只能读写已有表，不能建表；初始化数据库前必须先确认是否有 `service_role key` 或数据库密码。
- PowerShell 使用 `Invoke-RestMethod` 后，不能直接假设返回对象一定存在；拿 `id` 前先检查对象是否为 `$null`，否则清理临时数据时可能报错。
